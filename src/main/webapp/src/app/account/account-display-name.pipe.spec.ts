import {AccountDisplayNamePipe} from './account-display-name.pipe';

describe('AccountDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AccountDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
